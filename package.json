{
  "name": "beaver",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "scripts": {
    "build": "lerna run build",
    "clean": "pnpm recursive exec -- rm -rf node_modules",
    "start": "pnpm recursive run start --filter @beaver/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}